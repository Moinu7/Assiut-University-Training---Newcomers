#include <iostream>
#include <algorithm>

int main() {
    int a, b, c;
    std::cin >> a >> b >> c;

    int min_val = std::min({a, b, c});
    int max_val = std::max({a, b, c});

    std::cout << min_val << " " << max_val << std::endl;

    return 0;
}
