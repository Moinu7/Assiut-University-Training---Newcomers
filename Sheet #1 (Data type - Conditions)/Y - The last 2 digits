#include <iostream>

int main() {
    long long a, b, c, d;
    std::cin >> a >> b >> c >> d;

    long long product = (a % 100) * (b % 100) % 100 * (c % 100) % 100 * (d % 100) % 100;

    if (product < 10) {
        std::cout << "0" << product << std::endl;
    } else {
        std::cout << product << std::endl;
    }

    return 0;
}
